{#
  required values:
    time
  optional:
    notag - skip <time>
#}
{% set formatted = time|render|striptags|trim|replace({'am': 'a.m.', 'pm': 'p.m.', ':00': '' }) %}
{% if notag %}
  {{ formatted }}
{% else %}
  <time>{{ formatted }}</time>
{% endif %}
