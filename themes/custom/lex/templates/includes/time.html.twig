{#
  required values:
    time
  optional:
    notag - skip <time>
#}
{% set formatted = time | replace({'am': 'a.m.', 'pm': 'p.m.', ':00': '' }) %}
{{ notag ? formatted : '<time>' ~ formatted ~ '</time>' }}
