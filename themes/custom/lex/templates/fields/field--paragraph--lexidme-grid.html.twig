<div class="container-fluid lexid-container">    
    <div class="row">
        {% for item in items %}
            <div class="col-xs-4 grid-photo">{{item.content}}</div>
        {% endfor %}
    </div>
</div>