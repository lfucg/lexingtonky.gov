<?php
/**
 * Alter name suggestions for the blocks so subthemes will work.
 */
function lex_planning_commission_theme_suggestions_block_alter(&$suggestions, $variables) {
  foreach ($suggestions as &$suggestion) {
    $suggestion = str_replace('lex_planning_commission_', '', $suggestion);
  }
}
